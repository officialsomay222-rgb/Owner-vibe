## 2026-05-23 - Nested Button Interactivity via CSS :has()
**Learning:** Using an absolutely positioned hidden button () underneath interactive child elements with  allows for fully accessible 'clickable cards' without breaking valid nested-button HTML semantics. However, any intermediate wrapper s that encompass the interactive siblings (like album thumbnails) MUST NOT inadvertently retain , as this creates a click-blocking 'dead zone' over the hidden parent button underneath, effectively breaking the main card click action.
**Action:** When implementing hidden absolute buttons behind content, explicitly audit the CSS  hierarchy. Ensure intermediate structure wrappers are explicitly  and that  is applied ONLY to the specific, intended interactive child elements (like Play/Pause buttons) to allow clicks to fall through the dead space perfectly.
## 2024-05-23 - Nested Button Interactivity via CSS :has()
**Learning:** Using an absolutely positioned hidden button (`z-0 inset-0`) underneath interactive child elements with `pointer-events-auto` allows for fully accessible 'clickable cards' without breaking valid nested-button HTML semantics. However, any intermediate wrapper `div`s that encompass the interactive siblings (like album thumbnails) MUST NOT inadvertently retain `pointer-events-auto`, as this creates a click-blocking 'dead zone' over the hidden parent button underneath, effectively breaking the main card click action.
**Action:** When implementing hidden absolute buttons behind content, explicitly audit the CSS `pointer-events` hierarchy. Ensure intermediate structure wrappers are explicitly `pointer-events-none` and that `pointer-events-auto` is applied ONLY to the specific, intended interactive child elements (like Play/Pause buttons) to allow clicks to fall through the dead space perfectly.
